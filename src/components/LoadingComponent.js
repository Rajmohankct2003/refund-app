import React from 'react'
//component to display spinner when the content is loading
export default function LoadingComponent() {
    return (
        <div className="spinner">
            
        </div>
    )
}
